export const templateReadinessItems = [
  'Replace the template name with your project identity.',
  'Set app-level environment variables from the examples.',
  'Run pnpm validate before opening the first PR.',
] as const;
